console.log(Math.max(...((require('fs').readFileSync(require('path').resolve(__dirname, './input.txt'))).toString()).split('\r\n\r\n').map((str:string) => str.split('\r\n').map((str) => parseInt(str)).reduce((a,c) => a+c,0))));